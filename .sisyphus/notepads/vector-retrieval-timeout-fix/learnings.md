- `HybridRetrievalService.retrieve(...)` currently blocks on `lexicalFuture.join()` / `vectorFuture.join()`, so a stalled vector path is best locked with a daemon-thread budget harness in `HybridRetrievalServiceTest` before runtime changes.
- Existing `ChatAnswerServiceTest` already guards non-empty retrieval provenance semantics, so Task 1 can stay narrowly focused on service-level timeout degradation without changing chat tests.
- `HybridRetrievalService.retrieve(...)` can stay parallel while preserving lexical results by waiting for lexical completion first and then applying a bounded `vectorFuture.get(...)`; on timeout, cancelling the vector future and returning lexical-only satisfies the locked regression without changing controller response shape.
- `ArticleChunkVectorSyncService` uses Guava `ListenableFuture`, not `CompletableFuture`, so the startup vector timeout fix should wrap `collectionExistsAsync`, `createCollectionAsync`, `deleteAsync`, and `upsertAsync` with a shared bounded `future.get(timeout, TimeUnit.MILLISECONDS)` helper that cancels on timeout and returns a machine-checkable `[QDRANT_TIMEOUT] operation=... timeoutMs=...` detail.
- After a clean Maven rebuild, `ProductionStatusEndpointTest` still verifies Lucene-first partial-success semantics while `ArticleChunkVectorSyncOutageTest` can cover each timeout seam directly with a short overridden budget and pending `SettableFuture`s.
- `ProviderStatusService.qdrantStatus()` can keep the `/status/production` schema unchanged while still making timeout degradation machine-checkable by returning `DEGRADED` + `enabled=true` with a `[QDRANT_TIMEOUT]`-prefixed detail; config-disabled lexical mode remains the distinct `DISABLED` + `enabled=false` path.
- `HybridChatEndpointDegradedTest` and `ChatAnswerServiceTest` are enough to lock the semantic split without touching chat wording: use config-disabled snapshots for intentional lexical-only mode and reserve `[QDRANT_TIMEOUT]` details for timeout-driven degradation.
- Fresh Task 5 proof on 2026-04-24 used isolated home `/home/knifefire/work/ir_demo/workspace/vector-timeout-fix-runtime-20260424-110532` with the plan-lineage Qdrant collection `news_article_chunks_hybrid_20260423_1706`; the runtime reached `/status/production` HTTP 200 immediately and reported `lucene.documentCount=3` with Ollama/Qdrant providers AVAILABLE.
- The reopened startup seam was before Qdrant entirely: `ArticleChunkVectorSyncService.syncArticle(...)` calls `ArticleEmbeddingService.generateEmbeddings(...)` synchronously, so the repair needs its own 120s bound around `EmbeddingModel.embedForResponse(...)` rather than more Qdrant handling.
- A tiny `ExecutorService.submit(...).get(timeout, TimeUnit.MILLISECONDS)` seam in `ArticleEmbeddingService` is enough to bound embedding generation, cancel on timeout, and emit a stable `[EMBEDDING_TIMEOUT] operation=embed timeoutMs=... docId=...` detail while preserving empty-chunk / lexical-only short circuits and Lucene-first ingestion semantics.
- `ArticleChunkVectorSyncOutageTest` can cover the reopened gap without a new harness by injecting a mocked `EmbeddingModel` that blocks, overriding `embeddingTimeoutMillis()` to 25ms, and asserting `syncArticle(...)` returns a failed `SyncResult` containing the machine-checkable embedding-timeout detail.
- The startup lifecycle gap was that Tencent runs only retired when later code called `refreshTencentCrawlerStatuses()`. Launching each Tencent spider on a dedicated thread that calls `spider.run()` and then retires the run in `finally` makes run closure depend on actual spider completion instead of later polling.
- Keeping `refreshTencentCrawlerStatuses()` as a fallback but routing both paths through the same `retireTencentSpiderRun(runId)` helper preserves same-process overlap protection and existing `markStopped(...)`/`deriveOutcome(...)` semantics while removing the stale `RUNNING` window after spider completion.
- `CrawlerServiceStartupTest` can prove the lifecycle repair with a minimal seam: create a run, record one indexed document, invoke `onTencentSpiderCompleted(runId)`, and assert the observability snapshot moves that run to a completed `SUCCESS` state without any refresh-poll side effect.
- `ChatAnswerService.ask(...)` needed one shared `deadlineNanos` rooted at request start; reusing `remainingChatBudget(...)` for the main generation call and the single citation-repair retry keeps `/chat/ask` inside the same 120s envelope without changing degraded payload shape or provenance semantics when time remains.
- Focused regressions are enough to lock the reopened Task 5 seam: one service test where retrieval consumes most of the budget before generation, one service test where citation repair burns the remaining budget, and one endpoint test that still returns the existing machine-checkable degraded payload on timeout.
- Task 2 final fix:  now captures one retrieval-start deadline from , waits for lexical as the guaranteed floor, and only allows  to use the remaining budget; this removes the old lexical-latency-plus-fresh-timeout behavior while preserving the existing degraded message style.
- Task 2 final fix: HybridRetrievalService.retrieve now captures one retrieval-start deadline from irdemo.ai.retrieval.vector-timeout, waits for lexical as the guaranteed floor, and only allows vectorFuture.get to use the remaining budget; this removes the old lexical-latency-plus-fresh-timeout behavior while preserving the existing degraded message style.
- Fresh Task 5 rerun9 (2026-04-24 18:28 CST) on isolated home `/home/knifefire/work/ir_demo/workspace/vector-timeout-fix-task5-rerun9-20260424-182819` confirmed `/status/production` HTTP 200 immediately on port `18082`, and startup categories eventually retired to terminal outcomes (`edu=FAILED`, `tech=PARTIAL_SUCCESS`, `finance=PARTIAL_SUCCESS`) once the 120s embedding timeout path completed.
- Repair note (Task 2 reopen): `HybridRetrievalService.retrieve(...)` must await `lexicalFuture` first within the shared retrieval deadline, then give `vectorFuture.get(...)` only the remaining budget; waiting on vector first can incorrectly return empty `lexical_only` results when the vector path times out.
- Fresh Task 5 rerun10 (2026-04-24T19:43:15.276364+08:00) on isolated home `/home/knifefire/work/ir_demo/workspace/vector-timeout-fix-task5-rerun10-20260424-193533` showed startup ingestion now settles cleanly on a fresh non-demo process: `/status/production` reached HTTP 200 at 19:36:44 CST and by 19:42:36 CST all categories had terminal outcomes (`edu=FAILED`, `tech=PARTIAL_SUCCESS`, `finance=PARTIAL_SUCCESS`) with no lingering `RUNNING` states; see `.sisyphus/evidence/vector-timeout-fix-task5-rerun10-20260424-193533-status-settled.json`.
- Task 5 request-budget seam fix (2026-04-24T19:52:57.907206+08:00): keep the full configured retrieval deadline for lexical-first behavior, but subtract a small guard band only from the post-lexical vector wait and from remaining chat-model call budgets; this preserves the existing `120秒` degraded wording while returning before the wall-clock envelope instead of scraping it.
- Task 5 retrieval-only seam fix after rerun11 (2026-04-24T20:05:45.298462+08:00): `HybridRetrievalService` still needed more headroom for production-sized requests, so the post-lexical vector wait guard band now scales as `timeout/60` with the same 50ms floor and a 2s ceiling; this keeps lexical-first fallback intact, preserves the existing `向量检索执行超时，已在120秒后退化为词法检索` wording, and gives the live `/query/hybrid` path a larger pre-wall-clock cushion.
