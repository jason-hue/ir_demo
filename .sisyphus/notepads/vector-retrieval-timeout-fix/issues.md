- The exact focused Maven command reaches the new contract assertion and fails because blocked vector retrieval still does not degrade within the test budget; current failure site is `HybridRetrievalServiceTest.retrieveWithinBudget(...)` timing out.
- `lsp_diagnostics` reports broad pre-existing Lombok/model-resolution errors on the touched Java files, so Maven compile/test commands were used as the reliable verification source for this task after confirming the edited timeout path compiled and passed its focused regressions.
- The requested focused Maven test command initially failed against stale `target/` bytecode with missing Lombok-generated accessors (`Blog`, `ArticleChunkMetadata`, `AiProperties`, `Config`); `mvn clean` before rerunning restored a truthful verification state and the focused tests then passed.
- This task hit the same stale-bytecode trap again after the first focused test run; `mvn clean` was required before trusting the timeout/config-disabled regression results, even though the actual source fix was only a small `ProviderStatusService` probe-refactor follow-up.
- Fresh Task 5 runtime evidence shows the bounded request contract is still not met in this checkout: `POST /query/hybrid` for `腾讯新闻` and both required `POST /chat/ask` prompts each consumed the full 120s client budget and timed out (`.sisyphus/evidence/vector-timeout-fix-task5-*.json`).
- The same fresh runtime remained noisy on startup-ingestion settlement: by `.sisyphus/evidence/vector-timeout-fix-task5-status-after-probes.json` and `...status-late.json`, `tech` reached `SUCCESS` but `finance` and `edu` were still `RUNNING` with `finishedAt=null` more than 8 minutes after launch despite `indexedDocumentCount=1`, so Task 5 could not prove the new timeout containment fully resolved lingering RUNNING categories.
- For this reopened Task 3 repair, `mvn clean` remains a required first verification step in this repo; without it, stale compiled classes can again produce misleading timeout-regression failures unrelated to the actual embedding-timeout patch.
- The exact requested lifecycle-focused Maven test command is currently blocked by unrelated repo drift in test compilation (broken chat/demo/Qdrant tests) and by an existing `ProductionStatusEndpointTest` application-context failure where `IdxService` throws unresolved `Config#getIdx()` errors from current compiled main classes; the truthful narrow fallback was `mvn -Dtest=CrawlerServiceStartupTest -DfailIfNoTests=false surefire:test`, which passed and directly covers this task’s spider-retirement seam.
- This task hit the same stale-target problem on the exact requested focused Maven command: the first `mvn '-Dmaven.compiler.testIncludes=**/ChatAnswerServiceTest.java,**/HybridChatEndpointDegradedTest.java' -Dtest=ChatAnswerServiceTest,HybridChatEndpointDegradedTest test` failed in unrelated test compilation with many missing Lombok-generated accessors, and `mvn clean` was required before the exact command passed.
- Code review verdict candidate: REJECT. In `src/main/java/cn/edu/bistu/cs/ir/ai/HybridRetrievalService.java:92-94`, the code waits on `lexicalFuture.join()` before starting the full `vectorFuture.get(vectorRetrievalTimeoutMillis(), ...)` timeout window, so the overall `/query/hybrid` request can exceed the fixed 120s budget by lexical latency. This matches decisive rerun8 evidence where `.sisyphus/evidence/vector-timeout-fix-task5-rerun8-20260424-170143-query-hybrid-tencent.json` recorded `elapsedSeconds=121.234` even though the degraded reason says timeout fired at 120s. The current timeout test only uses near-zero lexical latency, so this end-to-end budget violation remains unguarded.
- Task 2 verification note: Java LSP stayed clean on HybridRetrievalService.java and HybridRetrievalServiceTest.java but still emitted repo-noise unused-field warnings on AiProperties.java; the authoritative verifier was mvn clean, the exact focused HybridRetrievalServiceTest command, and mvn -Dmaven.test.skip=true compile, all of which passed after the shared-deadline fix and new retrieval timeout property were wired in.
- Fresh Task 5 rerun9 evidence still does not satisfy the fixed 2-minute request budget: `.sisyphus/evidence/vector-timeout-fix-task5-rerun9-20260424-182819-query-hybrid-tencent.json` took `121.093s` before lexical-only timeout degradation, while both required chat probes returned machine-checkable degraded payloads only at about `120.04s`; startup no longer stayed indefinitely RUNNING, but the end-to-end retrieval/chat budget is still effectively right at or beyond the 120s threshold.
- Fresh Task 5 rerun10 (2026-04-24T19:43:15.276364+08:00) still fails the fixed request-budget proof on a clean non-demo process: `.sisyphus/evidence/vector-timeout-fix-task5-rerun10-20260424-193533-query-hybrid-tencent.json` took `121.011s` before returning lexical-only timeout degradation, while `.sisyphus/evidence/vector-timeout-fix-task5-rerun10-20260424-193533-chat-ask-gaode-why-citations.json` took `120.005s`; even though startup settlement now terminates, Task 5 remains blocked by retrieval/chat timing still landing at or just beyond the 120s envelope.
- Verification note (2026-04-24T19:52:57.907206+08:00): focused Maven verification is the reliable signal for this seam (`mvn clean`, `mvn -Dtest=HybridRetrievalServiceTest,ChatAnswerServiceTest test`, `mvn -Dmaven.test.skip=true compile`) because Java LSP still emits pre-existing Lombok/model-resolution false errors on these files even when the compile and focused tests pass.
- Rerun11 root-cause note (2026-04-24T20:05:45.298462+08:00): the prior retrieval-only fix still let a 120s timeout derive only about `1.2s` of vector-wait guard band, which matched `.sisyphus/evidence/vector-timeout-fix-task5-rerun11-20260424-195638-query-hybrid-tencent.json` finishing at `120.141s`; the focused fix was to increase the production-sized retrieval guard band rather than alter lexical-first control flow.
- Final-wave closeout blocker (2026-04-24): F1-F4 all approved, but `.sisyphus/plans/vector-retrieval-timeout-fix.md:308-310` explicitly forbids checking the final-wave boxes before the user gives explicit `okay`; remaining work is blocked only on that approval gate.
